# Supabase Configuration
# Get these values from https://supabase.com/dashboard/project/YOUR_PROJECT_ID/settings/api
SUPABASE_URL=https://your-project-ref.supabase.co
# Service Role Key - KEEP SECRET, never expose to clients. This key bypasses RLS.
SUPABASE_SERVICE_KEY=your-service-role-key-here

# =============================================================================
# LLM Provider Configuration
# =============================================================================
# Supported providers: azure_openai (default), openai, anthropic
LLM_PROVIDER=azure_openai

# Optional: Override the default model for the selected provider
# LLM_MODEL=gpt-4o

# --- Azure OpenAI (default provider) ---
AZURE_OPENAI_API_KEY=your-azure-openai-key-here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# --- OpenAI (alternative provider) ---
# To use OpenAI instead, set LLM_PROVIDER=openai and configure:
# OPENAI_API_KEY=sk-...

# --- Anthropic (alternative provider) ---
# To use Anthropic instead, set LLM_PROVIDER=anthropic and configure:
# ANTHROPIC_API_KEY=sk-ant-...

# --- LLM Parameters (optional) ---
# These use sensible defaults if not set
# LLM_TEMPERATURE=0.7
# LLM_MAX_TOKENS=2048

# Legacy (deprecated): Generic LLM key, use provider-specific keys above
# LLM_API_KEY=sk-...

# LangSmith (Optional - for observability)
LANGSMITH_API_KEY=ls-...
LANGSMITH_PROJECT=dangdai-api

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=true
